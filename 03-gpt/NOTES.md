# Day 4 — build GPT (Mon Aug 17)

Video: "Let's build GPT: from scratch, in code, spelled out" (1h56m)

Deliverable: my own attention + transformer block + full GPT, from memory.
Then: clone stanford-cs336/assignment1-basics (SEPARATE dir, not inside loki), wire my transformer into tests/adapters.py, start making tests green (uv run pytest).
